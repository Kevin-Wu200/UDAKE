# 地图引擎功能测试检查清单

## 测试准备

### 环境配置
- [ ] 已安装所有依赖（Leaflet 等）
- [ ] 已配置天地图 Token（如使用天地图模式）
- [ ] 已配置 ArcGIS API Key（如使用 ArcGIS 模式）
- [ ] 浏览器支持 ES6 模块

### 测试工具
- [ ] 已创建测试页面 `frontend/test-map-engine.html`
- [ ] 已创建测试工具 `frontend/js/utils/MapEngineTestHelper.js`
- [ ] 已创建切换脚本 `switch-map-engine.sh`

---

## 天地图模式测试

### 配置切换
```bash
./switch-map-engine.sh tianditu
```

### 1. 基础功能测试

#### 1.1 地图加载
- [ ] 打开应用，地图容器正常显示
- [ ] 天地图矢量底图正常加载
- [ ] 天地图注记图层正常显示
- [ ] 地图中心点位置正确（东京）
- [ ] 默认缩放级别正确（10）
- [ ] 控制台无错误信息

**测试方法：**
```
1. 启动应用: cd dist && npm start
2. 观察地图是否正常显示
3. 检查控制台输出
```

**预期结果：**
- 显示天地图矢量底图
- 显示中文地名注记
- 控制台输出 "✅ 天地图初始化完成"

#### 1.2 地图交互
- [ ] 鼠标滚轮缩放正常
- [ ] 鼠标拖拽平移正常
- [ ] 缩放控件正常工作
- [ ] 地图边界限制正常（minZoom: 3, maxZoom: 18）

**测试方法：**
```
1. 使用鼠标滚轮放大/缩小
2. 拖拽地图移动
3. 点击缩放控件
```

**预期结果：**
- 所有交互流畅无卡顿
- 缩放级别在 3-18 之间

#### 1.3 适配器接口
- [ ] `initMap()` 正常工作
- [ ] `getView()` 返回 Leaflet 地图对象
- [ ] 所有接口方法都已实现

**测试方法：**
```
打开浏览器控制台，执行：
window.adapter.getView()
```

**预期结果：**
- 返回 Leaflet Map 对象
- 无错误信息

### 2. 采样功能测试

#### 2.1 新建项目
- [ ] 点击"新建项目"按钮
- [ ] 填写项目名称
- [ ] 选择采样模式（自由采样）
- [ ] 选择坐标模式（经纬度）
- [ ] 项目创建成功

**测试方法：**
```
1. 点击"新建项目"
2. 输入项目名称: "测试项目"
3. 选择"自由采样"
4. 选择"经纬度输入"
5. 点击"创建"
```

**预期结果：**
- 项目面板显示
- 采样输入组件加载

#### 2.2 添加采样点
- [ ] 输入坐标（经度: 139.767125, 纬度: 35.681236）
- [ ] 输入值（100）
- [ ] 点击"添加采样点"
- [ ] 采样点显示在地图上
- [ ] 采样点样式正确（蓝色圆点，白色边框）

**测试方法：**
```
1. 在采样输入框中输入坐标和值
2. 点击"添加采样点"
3. 观察地图上的标记
```

**预期结果：**
- 地图上显示蓝色圆形标记
- 点击标记显示弹窗
- 弹窗显示坐标和值

#### 2.3 多个采样点
- [ ] 添加第 2 个采样点（139.8, 35.7, 值: 120）
- [ ] 添加第 3 个采样点（139.7, 35.6, 值: 80）
- [ ] 所有采样点都正确显示
- [ ] 采样点数量统计正确

**测试方法：**
```
重复添加采样点的步骤
```

**预期结果：**
- 地图上显示 3 个标记
- 采样点计数显示 "3"

### 3. 数据导入测试

#### 3.1 GeoJSON 文件上传
- [ ] 点击"数据上传"面板
- [ ] 选择测试 GeoJSON 文件
- [ ] 文件名正确显示
- [ ] 点击"上传数据"

**测试文件：** `frontend/test-boundary.geojson`

**测试方法：**
```
1. 点击文件选择器
2. 选择 test-boundary.geojson
3. 点击"上传数据"
```

**预期结果：**
- 显示字段映射弹窗
- 自动识别坐标和值字段

#### 3.2 字段映射
- [ ] 坐标字段自动识别
- [ ] 值字段自动识别
- [ ] 可以手动调整映射
- [ ] 点击"导入"

**测试方法：**
```
在字段映射弹窗中确认映射关系
```

**预期结果：**
- 字段映射正确
- 数据成功导入

#### 3.3 图层显示
- [ ] 采样点图层正确显示
- [ ] 地图自动缩放到数据范围
- [ ] 所有点都可见
- [ ] 点击点显示属性信息

**测试方法：**
```
观察地图上的点图层
```

**预期结果：**
- 所有采样点显示为蓝色圆点
- 地图视图包含所有点

### 4. 插值计算测试

#### 4.1 参数配置
- [ ] 选择克里金方法（普通克里金）
- [ ] 选择变异函数模型（球状模型）
- [ ] 输入网格分辨率（100）
- [ ] "开始插值"按钮已启用

**测试方法：**
```
在插值参数面板中配置参数
```

**预期结果：**
- 所有参数可以正常选择
- 网格分辨率校验通过

#### 4.2 任务提交
- [ ] 点击"开始插值"
- [ ] 任务成功提交
- [ ] 显示任务 ID
- [ ] 任务状态显示"运行中"

**测试方法：**
```
点击"开始插值"按钮
```

**预期结果：**
- 控制台输出任务 ID
- 任务状态面板更新

#### 4.3 任务轮询
- [ ] 进度条正常更新
- [ ] 任务状态实时更新
- [ ] 任务完成后显示"完成"

**测试方法：**
```
等待任务完成（约 10-30 秒）
```

**预期结果：**
- 进度从 0% 到 100%
- 状态从"运行中"到"完成"

### 5. 栅格图层测试

⚠️ **注意：** 天地图模式下栅格图层需要额外插件支持

#### 5.1 栅格加载尝试
- [ ] 插值完成后，检查控制台
- [ ] 查看是否有栅格加载提示
- [ ] 确认栅格 URL 是否正确

**测试方法：**
```
查看浏览器控制台输出
```

**预期结果：**
- 控制台输出警告信息
- 提示需要插件支持
- 显示栅格 URL

### 6. 图层控制测试

#### 6.1 采样点图层
- [ ] 取消勾选"采样点"
- [ ] 采样点图层隐藏
- [ ] 勾选"采样点"
- [ ] 采样点图层显示

**测试方法：**
```
在图层控制面板中切换开关
```

**预期结果：**
- 图层显示/隐藏正常
- 无延迟或闪烁

#### 6.2 其他图层
- [ ] 预测栅格开关正常（如有）
- [ ] 方差栅格开关正常（如有）

### 7. 点击查询测试

#### 7.1 点击采样点
- [ ] 点击地图上的采样点
- [ ] 显示信息弹窗
- [ ] 弹窗显示坐标
- [ ] 弹窗显示属性值

**测试方法：**
```
点击地图上的任意采样点
```

**预期结果：**
- 显示 Leaflet 弹窗
- 内容包含坐标和值

#### 7.2 点击空白区域
- [ ] 点击地图空白区域
- [ ] 不显示弹窗或显示坐标

**测试方法：**
```
点击地图上没有标记的区域
```

**预期结果：**
- 无错误信息
- 行为符合预期

### 8. 坐标系统测试

#### 8.1 坐标格式
- [ ] 输入经纬度坐标正常
- [ ] 坐标范围校验正常（经度 -180~180，纬度 -90~90）
- [ ] 坐标转换正常

**测试方法：**
```
输入不同的坐标值测试
```

**预期结果：**
- 合法坐标接受
- 非法坐标拒绝

### 9. 错误处理测试

#### 9.1 Token 未配置
- [ ] 控制台显示警告信息
- [ ] 应用不崩溃
- [ ] 地图仍可加载（Mock 模式）

**测试方法：**
```
使用默认的占位符 Token
```

**预期结果：**
- 控制台输出 "⚠️ 天地图 Token 未配置，当前为 Mock 模式"
- 应用继续运行

#### 9.2 网络错误
- [ ] 断开网络连接
- [ ] 应用显示错误提示
- [ ] 不影响已加载的功能

**测试方法：**
```
断开网络后刷新页面
```

**预期结果：**
- 显示加载失败提示
- 不出现白屏

---

## ArcGIS 模式测试

### 配置切换
```bash
./switch-map-engine.sh arcgis
```

### 1. 基础功能测试

#### 1.1 地图加载
- [ ] ArcGIS 地形底图正常加载
- [ ] 深色模式自动切换（如系统为深色模式）
- [ ] 控制台输出 "✅ ArcGIS 地图初始化完成"

#### 1.2 重复天地图模式的所有测试
- [ ] 地图交互测试
- [ ] 采样功能测试
- [ ] 数据导入测试
- [ ] 插值计算测试
- [ ] 栅格图层测试（应该正常工作）
- [ ] 图层控制测试
- [ ] 点击查询测试

### 2. ArcGIS 特有功能

#### 2.1 深色模式
- [ ] 切换系统深色模式
- [ ] 地图底图自动切换
- [ ] 界面样式适配

**测试方法：**
```
在系统设置中切换深色/浅色模式
```

**预期结果：**
- 底图自动切换
- 无需刷新页面

#### 2.2 栅格图层
- [ ] 预测栅格正常显示
- [ ] 方差栅格正常显示
- [ ] 栅格颜色正确
- [ ] 栅格透明度可调

**测试方法：**
```
完成插值计算后观察栅格图层
```

**预期结果：**
- 栅格图层正常显示
- 颜色渐变正确

---

## 切换能力测试

### 1. 天地图 → ArcGIS
- [ ] 使用天地图模式完成一次完整流程
- [ ] 切换到 ArcGIS 模式
- [ ] 重启应用
- [ ] 所有功能正常工作

**测试方法：**
```bash
./switch-map-engine.sh arcgis
cd dist && npm start
```

**预期结果：**
- 切换成功
- 功能一致

### 2. ArcGIS → 天地图
- [ ] 使用 ArcGIS 模式完成一次完整流程
- [ ] 切换到天地图模式
- [ ] 重启应用
- [ ] 所有功能正常工作

**测试方法：**
```bash
./switch-map-engine.sh tianditu
cd dist && npm start
```

**预期结果：**
- 切换成功
- 功能一致

### 3. 多次切换
- [ ] 连续切换 3 次
- [ ] 每次都能正常工作
- [ ] 无配置文件损坏

**测试方法：**
```bash
./switch-map-engine.sh tianditu
./switch-map-engine.sh arcgis
./switch-map-engine.sh tianditu
```

**预期结果：**
- 每次切换都成功
- 配置文件正确

---

## 自动化测试

### 使用测试工具
- [ ] 打开 `frontend/test-map-engine.html`
- [ ] 点击"运行所有测试"
- [ ] 查看测试结果
- [ ] 导出测试报告

**测试方法：**
```
在浏览器中打开 test-map-engine.html
```

**预期结果：**
- 所有自动化测试通过
- 生成 JSON 格式的测试报告

---

## 性能测试

### 1. 加载速度
- [ ] 记录天地图模式加载时间
- [ ] 记录 ArcGIS 模式加载时间
- [ ] 对比两者差异

**测试方法：**
```
使用浏览器开发者工具的 Network 面板
```

**预期结果：**
- 天地图模式更快
- 两者都在可接受范围内

### 2. 内存占用
- [ ] 记录天地图模式内存占用
- [ ] 记录 ArcGIS 模式内存占用
- [ ] 对比两者差异

**测试方法：**
```
使用浏览器开发者工具的 Memory 面板
```

**预期结果：**
- 天地图模式占用更少
- 无内存泄漏

### 3. 大数据量测试
- [ ] 导入 1000+ 个采样点
- [ ] 观察性能表现
- [ ] 检查是否有卡顿

**测试方法：**
```
准备包含大量点的 GeoJSON 文件
```

**预期结果：**
- 两种模式都能处理
- 性能可接受

---

## 兼容性测试

### 浏览器兼容性
- [ ] Chrome（最新版）
- [ ] Firefox（最新版）
- [ ] Safari（最新版）
- [ ] Edge（最新版）

### 操作系统兼容性
- [ ] macOS
- [ ] Windows
- [ ] Linux

---

## 测试报告

### 测试信息
- 测试日期：____________________
- 测试人员：____________________
- 测试环境：____________________

### 天地图模式测试结果
- 通过项目数：______ / ______
- 失败项目数：______
- 通过率：______%

### ArcGIS 模式测试结果
- 通过项目数：______ / ______
- 失败项目数：______
- 通过率：______%

### 发现的问题
1. ___________________________________________
2. ___________________________________________
3. ___________________________________________

### 总体评价
- [ ] 优秀 - 所有功能正常，无明显问题
- [ ] 良好 - 大部分功能正常，有少量小问题
- [ ] 一般 - 部分功能有问题，需要改进
- [ ] 需改进 - 存在较多问题，需要重大修复

### 建议
___________________________________________
___________________________________________
___________________________________________

---

## 附录

### 测试数据文件
- `frontend/test-boundary.geojson` - 测试边界文件
- `frontend/test-new-project.html` - 新建项目测试页面

### 测试工具
- `frontend/test-map-engine.html` - 自动化测试页面
- `frontend/js/utils/MapEngineTestHelper.js` - 测试工具类
- `switch-map-engine.sh` - 引擎切换脚本

### 相关文档
- `frontend/地图引擎切换说明.md` - 使用说明
- `frontend/测试指南.md` - 测试指南
- `实施总结.md` - 实施总结
