model:
  name: baseline_mlp
  task: spatial_interpolation
  input_dim: 4
  hidden_dims: [64, 64]
  output_dim: 1
  dropout: 0.1
training:
  optimizer: adam
  learning_rate: 0.001
  max_epochs: 50
  batch_size: 32
inference:
  batch_size: 128
  cache_ttl_seconds: 600
