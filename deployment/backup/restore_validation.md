# 备份恢复与完整性验证

## 验证步骤
1. 选择最近一次全量 + 增量备份。
2. 执行 `./deployment/scripts/test_backup_restore.sh`。
3. 校验数据一致性（记录条数、关键业务字段抽样）。
4. 校验配置一致性（环境变量、服务端口、安全参数）。
5. 校验日志完整性（时间窗口内日志连续）。

## 判定标准
- 恢复完成且健康检查通过。
- 校验文件 `SHA256SUMS` 全部验证成功。
- 恢复后关键 API 成功率 >= 99%。
