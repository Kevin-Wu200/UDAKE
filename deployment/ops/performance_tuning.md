# 性能调优文档

## 调优目标
- API P95 延迟 < 1.5s
- 5xx 错误率 < 1%
- CPU 平均利用率 40%~70%
- 内存利用率 < 80%

## 调优顺序
1. 先看基线：`deployment/autoscaling/performance_baseline.yml`
2. 再看瓶颈：CPU、内存、I/O、数据库慢查询。
3. 最后调策略：副本数、并发参数、缓存策略。

## 建议动作
- 高延迟：扩容应用副本、提高连接池、增加缓存命中。
- 高 CPU：优化热点接口、限制高开销任务并发。
- 高内存：降低单进程 worker 数，排查对象泄漏。
