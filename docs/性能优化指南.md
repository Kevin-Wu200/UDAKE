# 密钥验证性能优化指南

## 已落地项
- 服务端验证结果缓存（`SHA-256` 键，默认 `300s`）
- 前端内存 + localStorage 双层缓存（默认 `10min`）
- 验证性能指标采集（平均耗时、P95/P99）
- 用户/IP/密钥三级滑动窗口限流

## 关键指标
- `validation_count`
- `avg_response_time_ms`
- `p95_response_time_ms`
- `cache_hit_rate`

## 监控接口
- `GET /api/product-keys/validate/metrics`
