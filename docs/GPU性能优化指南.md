# GPU性能优化指南

## 1. 观察指标
- `performance.overall`：总运行次数、GPU运行次数、平均加速比。
- `memory_stats`：CPU/GPU 数据传输字节数与次数。

## 2. 优化建议
- 合理设置 `min_size_for_gpu`，避免小任务频繁上 GPU。
- 对批量预测任务优先合并请求，提高并行度。
- 持续关注 `recent` 列表中的慢操作，做专项优化。

## 3. 验证方式
- 对同一任务分别使用 CPU/GPU 跑基准。
- 比较耗时、误差和资源占用。
